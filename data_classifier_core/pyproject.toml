[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "data_classifier_core"
version = "0.1.0"
requires-python = ">=3.11"
description = "Zone detection engine — Rust core with Python bindings"

[tool.maturin]
features = ["python"]
